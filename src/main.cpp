//#include "graph.h"
#include "iostream"
#include "SQLite.h"

int main() {
//    BaseGraph graph(10);
//    for (int i = 0; i < graph.Size(); ++i) {
//        std::cout << graph.GetAdj(i).size() << '\n';
//    }
}
